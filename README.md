# 💰 AkFinance — персональный финансовый менеджер

|  |
|---|
| **AkFinance — это fullstack веб-приложение для личных финансов с полноценным бэкендом, базой данных, ролевой моделью по пользователям, жизненным циклом транзакций, бюджетами, аналитическим дашбордом, котировками криптовалют в реальном времени и встроенным AI финансовым советником на базе GPT-4o mini.** |

## ✨ Коротко о продукте

Приложение закрывает полный цикл персонального учёта финансов:

- 🔐 Пользователь **регистрируется** и получает изолированный аккаунт  
- 🏷️ Создаёт **категории** расходов и доходов с иконкой и цветом  
- 💳 Ведёт **транзакции** — доходы, расходы, переводы — с заметками и датой  
- 📆 Планирует **бюджеты** по категориям на каждый месяц  
- 📊 Смотрит **дашборд** с ключевыми метриками, пай-чартом и прогрессом бюджетов  
- 🪙 Следит за **рынком криптовалют** с реальными котировками и историческими графиками  
- 🤖 Общается с **AI советником** прямо внутри приложения  
- 🎛️ Настраивает **тему, язык и валюту** под себя  

## 🧰 Технологии

### Backend
- **Java 21** — основной язык  
- **Spring Boot 3.2.3** — фреймворк приложения, встроенный Tomcat 10  
- **Spring Security 6** — авторизация через JWT фильтр цепочку  
- **Spring Data JPA + Hibernate 6.4** — ORM, JPQL запросы, Specifications для динамической фильтрации  
- **Flyway 9.22** — версионированные миграции схемы БД  
- **HikariCP** — пул соединений (встроен в Spring Boot)  
- **JJWT 0.12.5** — создание и валидация JWT токенов (HMAC-SHA)  
- **BCrypt** — необратимое хэширование паролей  
- **Lombok** — автогенерация геттеров, сеттеров, билдеров, конструкторов  
- **Bean Validation (Jakarta)** — декларативная валидация запросов через `@Valid`  
- **Micrometer + Prometheus** — метрики, `/actuator/prometheus`  
- **Maven** — сборка, зависимости, lifecycle  

### Frontend
- **React 19** — UI фреймворк, функциональные компоненты, хуки  
- **TypeScript 5.9 (strict)** — строгая типизация, `noImplicitAny`, `strictNullChecks`  
- **Vite 7** — сборщик нового поколения, нативные ES модули, HMR, proxy на бэкенд  
- **TailwindCSS 4** — утилитарные стили, конфиг через CSS, tree-shaking классов  
- **TanStack Query 5** — серверный стейт-менеджмент, кэш, авто-рефетч, инвалидация  
- **React Hook Form 7** — uncontrolled формы без лишних ре-рендеров  
- **Zod 4** — TypeScript-first схемы валидации, интеграция с RHF через resolver  
- **React Router 7** — клиентский роутинг, PrivateRoute guard  
- **Recharts 3** — SVG-графики: PieChart для дашборда, AreaChart с градиентом для рынков  
- **i18next 25 + react-i18next** — локализация RU/EN, неймспейсы, TypeScript типизированные ключи  
- **Sonner 2** — тост-уведомления  
- **Lucide React** — иконки, tree-shakeable компоненты  

### Инфраструктура
- **PostgreSQL 16** (alpine)  
- **Docker + Docker Compose** — контейниризация БД  
- **OpenAI API** (gpt-4o-mini) — AI советник  
- **CoinGecko API** (публичный, без ключа) — котировки криптовалют  

## 🏗 Архитектура

```text
AkFinance/
├── apps/
│   └── web/                    # React + Vite фронтенд
│       ├── src/
│       │   ├── api/            # client.ts, types.ts — HTTP клиент и DTO типы
│       │   ├── pages/          # Страницы приложения
│       │   ├── components/     # Shared UI компоненты, AiChatWidget
│       │   ├── contexts/       # AuthContext, ThemeContext
│       │   └── locales/        # ru.ts, en.ts — переводы
│       └── vite.config.ts      # Proxy /api -> :8080, алиас @, плагины
│
├── services/
│   └── api/                    # Spring Boot бэкенд
│       └── src/main/java/com/akfinance/api/
│           ├── controller/     # REST контроллеры
│           ├── service/        # Бизнес-логика
│           ├── repository/     # Spring Data JPA репозитории
│           ├── domain/
│           │   ├── entity/     # JPA сущности
│           │   └── enums/      # TransactionType, CategoryType, Theme
│           ├── dto/            # Request/Response DTO по модулям
│           ├── security/       # JWT, фильтр, SecurityConfig
│           └── exception/      # GlobalExceptionHandler, кастомные исключения
│
└── infra/
    └── docker/
        └── docker-compose.yml  # PostgreSQL :5433
```

Ключевой принцип: **изоляция данных по `userId`**. Каждый запрос к БД фильтруется по идентификатору текущего пользователя, extracted из JWT. Пользователь физически не может получить данные чужого аккаунта.

## 🗃 Доменные сущности (БД)

Схема управляется через Flyway, три миграции:

```text
V1__create_users.sql                      — пользователи
V2__create_categories_transactions.sql    — категории + транзакции + 4 индекса
V3__create_budgets_preferences.sql        — бюджеты + настройки + 2 индекса
```

Основные таблицы:

- **users** — email, password_hash, created_at  
- **categories** — user_id, name, type (INCOME/EXPENSE), icon, color. UNIQUE(user_id, type, name)  
- **transactions** — user_id, type (INCOME/EXPENSE/TRANSFER), amount, currency, occurred_at, category_id, note  
- **budgets** — user_id, category_id, month, year, limit_amount, currency. UNIQUE(user_id, category_id, month, year)  
- **user_preferences** — user_id (PK = FK), locale, theme, default_currency  

## 🔄 Жизненный цикл данных

### Транзакция
```text
Пользователь создаёт транзакцию (INCOME/EXPENSE/TRANSFER)
    → валидация @Valid (amount > 0, type обязателен, occurredAt обязателен)
    → привязка к категории (опционально, ON DELETE SET NULL)
    → сохранение в БД с автогенерацией UUID и timestamp
    → инвалидация кэша дашборда на фронтенде
    → дашборд пересчитывает метрики за месяц
```

### Бюджет
```text
Пользователь создаёт бюджет на категорию + месяц + год
    → UNIQUE constraint защищает от дублей
    → при запросе бюджетов считается spent = SUM транзакций этой категории за период
    → progressPercent = (spent / limitAmount) * 100
    → UI красит прогресс-бар: зелёный < 80%, жёлтый >= 80%, красный >= 100%
```

## 🔐 Аутентификация и безопасность

Один режим входа: **Email + Password**.

**Регистрация:**
- Валидация email и пароля через `@Valid`  
- Пароль хэшируется через BCrypt (необратимо, с солью)  
- Создаётся `User` + `UserPreference` с дефолтами (locale=ru-RU, theme=LIGHT, currency=RUB)  
- Выдаётся JWT токен (срок жизни: **1 час**)  

**Авторизация каждого запроса:**
- `JwtAuthenticationFilter extends OncePerRequestFilter` — перехватывает все запросы  
- Извлекает `Authorization: Bearer <token>` из заголовка  
- Валидирует подпись через HMAC-SHA ключ из `application.yml`  
- Если токен валидный — помещает `UUID userId` в `SecurityContextHolder`  
- `SecurityUtils.getCurrentUserId()` — используется в контроллерах для изоляции данных  

**Дополнительно:**
- Каждый запрос получает `X-Request-ID` заголовок (8 символов UUID) через MDC — для трассировки в логах  
- Единый GlobalExceptionHandler с form-friendly ответами: `ValidationError`, `ResourceNotFound`, `DuplicateResource`, `BadCredentials`, `InternalError`  
- CORS настроен явно: `http://localhost:5173`, методы GET/POST/PUT/DELETE/OPTIONS  

## 🌐 Backend API

Базовый URL: `http://localhost:8080/api/v1`

### Auth — `/auth`
```text
POST /auth/register    — регистрация, возвращает {accessToken, expiresIn}
POST /auth/login       — вход, возвращает {accessToken, expiresIn}
```

### Transactions — `/transactions`
```text
GET    /transactions                    — список с пагинацией и фильтрами
GET    /transactions/{id}               — одна транзакция
POST   /transactions                    — создать
PUT    /transactions/{id}               — обновить
DELETE /transactions/{id}               — удалить

Query-параметры GET: page, size, type, categoryId, from, to, minAmount, maxAmount, q
```

Ответ GET списка — объект `Page<TransactionResponse>` со Spring-совместимой структурой (content, totalElements, totalPages, number, first, last).

Для строгой фильтрации используется паттерн **JPA Specification** — динамическая сборка WHERE условий. Если фильтров нет — простой запрос по `userId`. Если есть комбинация — Specification собирает `Predicate` из ненулевых параметров.

### Categories — `/categories`
```text
GET    /categories?type=EXPENSE    — список категорий (опционально фильтр по типу)
POST   /categories                 — создать
PUT    /categories/{id}            — обновить
DELETE /categories/{id}            — удалить
```

### Budgets — `/budgets`
```text
GET    /budgets?month=2&year=2026  — бюджеты с расчётом spentAmount и progressPercent
POST   /budgets                    — создать
PUT    /budgets/{id}               — обновить лимит
DELETE /budgets/{id}               — удалить
```

### Dashboard — `/dashboard`
```text
GET /dashboard/summary?month=2&year=2026   — сводка: totalIncome, totalExpense, balance,
                                              topCategories (топ 5), budgets (превью)
```

Сервис Dashboard рассчитывает всё одним проходом: суммирует INCOME и EXPENSE через JPQL `COALESCE(SUM(...), 0)`, получает топ категорий через `GROUP BY + ORDER BY SUM DESC`, подтягивает превью бюджетов.

### Preferences — `/preferences`
```text
GET   /preferences     — настройки пользователя
PUT   /preferences     — обновить locale, theme, defaultCurrency
```

### Служебные
```text
GET /actuator/health      — статус приложения
GET /actuator/metrics     — JVM и HTTP метрики
GET /actuator/prometheus  — Prometheus scrape endpoint
```

## 🧭 Frontend: маршруты и страницы

```text
/login           — вход в аккаунт
/register        — регистрация

/                — Dashboard: KPI карточки + donut chart + бюджеты
/transactions    — таблица транзакций с фильтрами, пагинацией, running balance
/budgets         — бюджеты с прогресс-барами по месяцу
/categories      — CRUD категорий, разделено на Доходы / Расходы
/reports         — расширенная аналитика
/markets         — котировки 15 криптовалют, watchlist, area chart по периодам
/calculator      — финансовый калькулятор
/settings        — тема, язык, валюта
/profile         — профиль пользователя
```

Все маршруты кроме `/login` и `/register` защищены через `PrivateRoute` — если JWT токена нет в localStorage, происходит redirect на `/login`.

## 🤖 AI Финансовый советник

Встроен в каждую страницу как плавающий чат-виджет в правом нижнем углу.

**Как работает:**
- Кнопка запуска — золотая с пульсирующей зелёной точкой, при клике открывается чат-панель (520px высотой)  
- Запросы идут напрямую с фронтенда на `api.openai.com/v1/chat/completions`  
- Модель: **GPT-4o mini** (`temperature: 0.7`, `max_tokens: 500`)  
- API ключ: `VITE_OPENAI_API_KEY` из файла `.env.local`  
- System prompt задаёт роль: профессиональный финансовый советник, отвечает по-русски, с конкретными числами и упоминанием рисков, умеет анализировать акции, крипту, ETF, недвижимость, депозиты, облигации  
- Полный контекст диалога передаётся при каждом запросе — AI помнит историю сессии  
- При первом открытии показываются **quick prompts**: "Куда вложить 100 000 ₽?", "Стоит ли покупать Bitcoin сейчас?", "Как защитить деньги от инфляции?", "Что такое диверсификация?"  
- Кнопка "Очистить" — сбрасывает диалог до приветственного сообщения  

## 📈 Страница Рынков

Интеграция с **CoinGecko API** (публичный, без ключа, с авто-рефетчем каждые 30 секунд):

- Список **15 монет**: Bitcoin, Ethereum, BNB, Solana, XRP, Cardano, Avalanche, Polkadot, Chainlink, TON, DOGE, SHIB, LTC, UNI, XLM  
- **Watchlist** — звёздочка на монете добавляет её в персональный тикер вверху страницы  
- **Горизонтальный тикер** — карточки монет из watchlist с текущей ценой и изменением за 24ч  
- **Детальный вид монеты** — логотип, цена, % изменение, выбор периода (24ч / 7д / 30д / 1г)  
- **Area Chart** (Recharts) с градиентной заливкой — зелёный при росте, красный при падении  
- Данные графика прореживаются до 80 точек через `Math.floor(prices.length / 80)` для производительности  
- Рыночная капитализация, объём торгов за 24ч, тикер монеты  

## 🎨 Дизайн-система

Tema построена на **CSS переменных** в `index.css`:

```text
--color-gold        — основной акцентный цвет
--color-bg          — фон страницы
--color-surface     — фон карточек
--color-surface-2   — фон вторичных элементов и инпутов
--color-text        — основной текст
--color-text-muted  — приглушённый текст
--color-border      — рамки
--color-success     — зелёный (#22c55e)
--color-danger      — красный (#ef4444)
--color-warning     — жёлтый
--shadow-sm/md      — тени
```

Переключение темы (LIGHT/DARK) — установка класса на `<html>`, сохранение в `user_preferences` через бэкенд. TailwindCSS используется для layout утилит, кастомные CSS переменные — для runtime-динамических цветов.

## 🔍 Архитектурные решения

**Почему JPA Specification для фильтрации?** Потому что количество активных фильтров на странице транзакций неизвестно заранее. Specification позволяет динамически собирать `Predicate` из тех параметров, что реально переданы. Альтернатива — JPQL с кучей `IF ... AND` — нечитаема.

**Почему TanStack Query вместо Redux?** Большинство стейта в SPA приложениях — это серверные данные. TanStack Query решает точно эту задачу: кэш, инвалидация, загрузка, ошибки — без единой строки boilerplate. Redux имело бы смысл для сложного клиентского стейта, которого здесь нет.

**Почему `ddl-auto: validate` а не `create-drop`?** Потому что `create-drop` уничтожит все данные при каждом рестарте. `validate` — безопасный режим: Hibernate проверяет что схема соответствует entities, но ничего не меняет. Все изменения схемы — только через Flyway миграции.

**Почему токен в localStorage а не в cookie?** localStorage проще для SPA без SSR. Cookie с HttpOnly были бы безопаснее против XSS, но требуют дополнительной настройки CSRF. Для MVP с одним доменом localStorage — разумный компромисс.

**Почему `COALESCE(SUM(...), 0)` в репозитории?** Потому что `SUM` на пустой выборке возвращает `NULL`, а не `0`. Без COALESCE сервис получил бы `null` вместо `BigDecimal`, что привело бы к NullPointerException при вычислении `income.subtract(expense)`.
| **ПРИМЕР** |
|---|
| **AkFinance Обзор**<br><img width="1920" height="1080" alt="Снимок экрана (888)" src="https://github.com/user-attachments/assets/91901ca4-9486-4711-aab8-0a222a5a27c1" /> |
| **AkFinance Транзакции**<br><img width="1920" height="1080" alt="Снимок экрана (889)" src="https://github.com/user-attachments/assets/91b0359d-b834-4f89-98e2-c31fa8894fb1" /> |
| **AkFinance Рынки**<br><img width="1920" height="1080" alt="Снимок экрана (890)" src="https://github.com/user-attachments/assets/7f29b42a-e69b-4f70-8df9-7fd3c1013a9e" /> |
| **AkFinance Анализ**<br><img width="1920" height="1080" alt="Снимок экрана (891)" src="https://github.com/user-attachments/assets/859048f9-a18f-4e07-b1b6-b224f41d890d" /> |
| **AkFinance Калькулятор**<br><img width="1920" height="1080" alt="Снимок экрана (892)" src="https://github.com/user-attachments/assets/58b20daa-d5a6-4fe3-b6ef-43defe5ae427" /> |
| **AkFinance Инвестиции + AI ассистент**<br><img width="1920" height="1080" alt="Снимок экрана (893)" src="https://github.com/user-attachments/assets/967cf4a5-3a21-4dee-807d-bd1decfc1328" /> |
| **AkFinance Профиль**<br><img width="1920" height="1080" alt="Снимок экрана (894)" src="https://github.com/user-attachments/assets/3374d753-03ba-4c19-a906-d8001e7f5aac" /> |
